# 开发准则

本准则适用于本项目的开发者及 AI 代理。

## 提示词整理与表达

- 收到任务后，先将用户提示词整理为更规范、清晰的表述，再开展执行。调整语序、分段和组织结构时，完整保留原始信息、语义、背景、目标、约束、示例、优先级、交付范围和疑问，避免因提炼、压缩或概括造成信息流失。
- 整理后对照原提示词检查完整性，保留不确定性和条件关系；存在歧义时明确标注，避免擅自补充结论或改变意图。
- 将用户已确定的要求、代理补充建议和待确认事项清楚区分；收到后续补充时同步更新规范化表述与相关文档。
- 沟通、文档和交付说明以直接陈述句为主。严禁使用“不是……而是……”及类似的否定转折句式。

## 美术素材制作

- 项目美术素材主要由 AI 代理调用生图模型生成，具体方向遵循 [美术风格与素材制作](Docs/美术/美术风格与素材制作.md)。
- 制作前查阅相关模块文档，明确用途、风格和规格；生成后检查一致性、可读性和游戏适用性，并将采用的素材与必要制作信息归入项目。
- 文档中的设计方向与实际素材完成状态应清楚区分；实际素材生成按具体制作任务开展。

## 项目文档与实现流程

- 开始阶段相关实现前，阅读 [项目阶段与当前进度](Docs/游戏整体/项目阶段与当前进度.md)，核对当前目标；范围和实际进度变化时同步更新该文档。

- 必须遵循 [项目文档规范](Docs/项目规范/项目文档规范.md)。
- **实现前必须先检查 `Docs/` 下是否存在与当前任务相关的文档，并阅读相关内容及必要引用，再开始实现或修改。** 按模块、功能名称和关键术语检索，确认当前规则、表现要求、实现状态和依赖关系。
- 主要文档存放在 `Docs/`，按模块或主题创建子目录，不将具体文档全部放在一级目录。
- 一个文档应是完整的整体，同一功能的策划、美术和程序说明按需整合，不按工种拆成策划案、设计案和技术文档。允许整体玩法设计、世界观设计等主题完整的单一用途文档。
- 文档文件名称和标题必须以中文为主；技术术语、代码标识符及约定的入口文件名可以保留原文。
- 变更实现时必须同步更新相关文档，使其对齐项目最新状态；明确区分已实现内容、待实现设计和待确认事项。
- 删除或改写已失效的说明，避免过时信息占用上下文；历史追溯使用 Git 和 PR，不在现行文档中堆积旧版本。
- 没有相关文档时，结合任务要求与项目现状开展工作；需要长期维护的说明应在对应模块目录创建，并保持主题完整。

## 平台、存档与扩展约束

- 游戏目标上线 Steam，联机基于 Steam 并采用 Listen Server，同时支持创意工坊。实现前查阅 [发行与创意工坊](Docs/平台与扩展/发行与创意工坊.md)，在平台接入、内容注册、版本兼容和扩展加载方面预留边界；具体工具与开放范围按文档中的待确认事项推进。
- 一档一角色，单人与联机沿用长期培养的角色。涉及保存、成长、物品和房间切换时，遵循 [角色存档与长期成长](Docs/角色系统/角色存档与长期成长.md)，区分角色进度、世界进度和临时状态。

## 通过 PR 开发

- 代码、资源和文档变更均优先通过 Pull Request（PR）提交和合并。
- 从最新的 `main` 创建独立工作分支，在该分支完成修改、提交和验证，再推送工作分支并创建 PR。
- 尽量避免直接向 `main` 推送变更；推送工作分支以创建或更新 PR 属于正常流程。

## PR 使用中文

- PR 标题统一采用 `关键词：中文描述` 格式，使用英文关键词和中文冒号，描述应清楚概括本次变更。
- 示例：`docs：明确pr开发流程`、`fix：修复ui bug`。
- 常用关键词：`docs`（文档）、`fix`（修复）、`feat`（新功能）、`refactor`（重构）、`test`（测试）、`chore`（维护）。
- PR 描述以中文为主，说明变更目的、主要内容及验证情况。
- 技术术语、代码标识符、命令和文件路径可保留原文。
