# 主菜单

状态：本次独立 PR 实现中。项目暂名 Project Tensei。

主菜单以最新存档所属角色的当前成长插画作为背景，转职后可使用对应插画。初版使用洛恩已确认的专注练剑原画。存档和插画解锁系统尚待实现，初版保留背景解析入口及默认回退。

可点击菜单放在左侧，采用暗色渐变遮罩保持文字与插画清晰。支持鼠标和键盘焦点，窗口缩放时保持可读性。角色脸部优先保留在画面右侧。

本次范围为可启动的主菜单、角色入口展示及基础设置。第一阶段最高目标调整为剑士移动与动画正常播放，武器和攻击延期。角色创建、存档写入与移动场景按后续实现推进。
