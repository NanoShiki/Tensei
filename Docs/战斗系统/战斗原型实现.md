# 战斗原型实现

状态：2D 简化回合战斗与逐层地图 Demo 已实现。主菜单 → 开始旅程 → 逐层探索或战斗演示。

## 实现组成

| 路径 | 职责 |
| --- | --- |
| `Scripts/Character/character_library.gd` | 洛恩初始 HP、AC、命中、敏捷、药水库存 |
| `Scripts/Battle/ability_library.gd` | 稳定能力 ID，目标、效果、骰点或药水库存字段 |
| `Scripts/Battle/battle_state.gd` | 先攻、统一行动次数、目标校验、骰点、结算 |
| `Scripts/Battle/battle_screen.gd` | 战斗界面、点选、反馈、地图显示与结果入口 |
| `Scripts/Exploration/floor_run.gd` | 每层有向节点图、历史与实际路线、跨层节点标识、节点一次结算与资源延续 |
| `Scripts/Core/game_flow.gd` | 菜单入口与会话状态 |
| `Assets/Battle/` | 透明角色图与技能图标 |

## GM 测试菜单

当前 Demo 由 `GameFlow` 创建一个常驻的 `Scripts/Debug/gm_panel.gd`（CanvasLayer）。右上角 GM 悬浮按钮展开菜单，关闭按钮或 Esc 收起。指令位于 ScrollContainer，条目较多时可滚轮上下滑动。当前只有“秒杀当前敌人”，因此通常不会出现滚动条。

秒杀不消耗行动、药水或随机数，通过战斗状态判定胜利，再调用现有场景结算及刷新流程。探索节点会正常完成，重复点击不重复回写。主菜单、地图、已结束战斗及动作反馈播放期间禁用，并显示原因；动作结束后自动恢复。菜单展开时屏蔽战斗快捷键，菜单不暂停游戏。

扩展入口：`register_command(id, title, description, execute, reason)`。每条指令使用唯一 ID、中文标题／说明、返回成功与否的执行 Callable，以及返回不可用原因字符串的 Callable（空串表示可用）。面板只负责登记、可用性、滚动与执行反馈；具体模块实现操作与校验，执行前再次检查。失效回调会禁用。当前战斗上下文通过 `gm_battle_context` 分组查找，场景切换后不会沿用已释放节点。

当前 GM 入口在 Demo 中默认启用，供开发验收；当前没有命令行、正式权限体系或联网 GM 指令。后续正式发行时按发行方案决定是否保留入口。

验收步骤：

1. 本 PR 分支在 Godot 按 F5，主菜单右上角点击 GM，应展开菜单，“秒杀当前敌人”显示“请先进入战斗”并禁用。
2. 收起后选择“开始旅程”→“逐层探索”→战斗节点，等待当前动作结束，再展开 GM 并点“秒杀当前敌人”。应出现正常胜利面板，己方资源不因 GM 操作减少。
3. 再次点击不会重复结算；收起菜单后返回地图，当前战斗节点已完成，下一步路线可用。独立战斗演示也可使用。
4. 点击关闭或按 Esc 收起；重新进入其他场景仍只有一个 GM 按钮。960×540 下按钮和菜单应完整可见。
5. 自动化通过注册多条测试指令验证滚动与扩展（测试条目不进入实际 Demo）。运行 `godot --headless --path . --script Tests/test_gm_panel.gd`，预期 `GM PANEL CHECKS: 0 failures`。

本轮 Windows Godot 4.7.2：GM 专项及原有战斗逻辑、地图、战斗界面、主菜单共五组测试通过；小窗口实际渲染检查通过。用户操作体验和 macOS 待验收。

## 战斗接口与顺序

`setup(character, floor_number, seed_value)` 拷贝角色数据；随机数种子可用于测试复现，正常游玩随机初始化。开战决定固定先攻顺序。

`reason(id)` 查询不可用原因；`use_ability(id, target)` 先验证回合、能力、行动、库存与目标，全部合法后扣一次行动并执行效果。攻击命中失败也消耗行动；非法选择不消耗行动、物品或随机数。`grant_actions(amount)` 为特殊效果增加当前行动次数，不跨回合积攒。

药水通过 `target`、`effect`、`amount`、`stock` 配置直接影响单位。治疗与伤害共享结算入口，无环境中间实体。`end_turn()` 主动结束己方回合；`enemy_turn()` 执行敌人一次攻击并推进。`outcome` 为 ongoing、victory 或 defeat。

界面在异步敌人提示和结果浮字期间锁定操作，结算只回写一次。战斗结束前地图不能进入其他节点，胜利返回地图后才解锁下一步。

## 地图与会话

地图参数为探索种子、楼层序号和总层数；默认 30 层，每层使用同一拓扑模板并变化部分节点。局部节点 ID 用于当前地图连线，完整 key 组合远征 ID、层号和局部节点 ID。每次 setup 创建新远征身份；同种子仍可生成同一地图。历史楼层与实际通过路线驻留内存，floor_snapshot 返回隔离副本。字段含义见 [远征状态与路线保留](../世界与探索/地下城推进与怪物之宴.md)。

`enter(id)` 校验连线与完成状态；战斗节点记入 pending，胜利后才设为当前位置。`finish_battle(hero, victory)` 保存生命与两类药水，防重复结算。出口保留原层并生成下一层或完成探索；重复 generate_floor 不会清空已生成的当前层。失败标记为本趟终止，重新 setup 后才能开始新旅程。数据只驻内存，无正式存档、网络、掉落背包或 Steam 接入。

## 验证

- `Tests/test_battle_logic.gd`：每回合一次行动，药水与攻击共用次数，额外行动，非法目标无副作用，闪避时效，药水库存，先攻回合，暴击/未命中，胜负和 30 层可达性、资源延续、重复奖励防护。
- `Tests/test_floor_run.gd`：跨层历史、真实分支路线、节点标识、快照隔离、待结算锁定、失败终止、重新出发与重复奖励防护。
- `Tests/test_battle_screen.gd`：技能选择与取消、目标限制、实际点击、结束回合、药水和胜利面板、地图进出及小窗口缩放。
- `Tests/test_main_menu.gd`：主菜单原有交互回归。
- `Tests/capture_battle.gd`：渲染检查，可用 `TENSEI_CAPTURE_PATH` 指定截图；后续参数见脚本。

运行方式：`godot --headless --path . --script Tests/test_battle_logic.gd`；界面测试同理替换脚本路径。截图使用非 headless 渲染；`TENSEI_CAPTURE_MODE=target` 选择灼烧药水目标，`map` 截图地图，`TENSEI_CAPTURE_SMALL=1` 使用 960×540 窗口。

## 当前限制

仅有洛恩与哥布林，角色采用单帧站姿配合浮字，没有骨骼动画、独立武器挂接或完整攻击动作。装备只读。数值未平衡。多队员、反应/豁免等扩展、持久化、联机和正式地牢内容待后续评估。

## 本次验收记录

Godot 4.7.2：战斗/地图逻辑、战斗界面与主菜单三组测试均通过。1280×720 战斗点选与 960×540 战斗、地图完成实际渲染检查，无界面裁切。文档相对链接检查、`git diff --check` 通过。角色新素材已完成代理自检与引擎显示检查，用户美术认可和长期游玩平衡待验收。

2026-09-26：Windows Godot `4.7.2.stable.custom_build.ed1daf0bf` 下新增远征历史测试及原有战斗／地图、战斗界面、主菜单测试全部通过。本轮只调整远征状态，无布局或素材改动，未重新进行截图与用户验收。
